library contstants;

final xLineName = ["A", "B", "C", "D", "I", "F", "G", "H"];
final yLineName = [1, 2, 3, 4, 5, 6, 7, 8];

enum Course{
  up,
  down
}

enum Color {
  black,
  white
}

enum Side {
  top,
  bottom
}